{
  "scripts": {
    "dev": "nodemon index.js",
    "test": "jest --watch --verbose",
    "coverage": "jest --watch --coverage --verbose"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "jest": "^28.1.3",
    "nodemon": "^2.0.19",
    "supertest": "^6.2.4"
  }
}
